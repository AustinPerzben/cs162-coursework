"use strict";
$('#myModal').on;
'show.bs.modal', (e) -  >
    data;
status: 200, msg;
'Good This is';
if (!data)
    return e.preventDefault();
#;
stops;
modal;
from;
being;
shown;
$('#myModalLabel').html("This is your status: <span class='color'>" + data.status + "</span>");
$('.modal-body').text(data.msg);
return;
